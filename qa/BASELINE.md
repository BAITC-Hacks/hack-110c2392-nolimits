# Исходный отчёт QA

Проверенная версия приложения: `91e6689ce5cd3518722d80a7b0f1ea80504580c5` (`fix: stabilize demo startup and outlier detection`). Дата: 23.09.2026, прогон начат в 14:15:53 UTC+5. Среда: Windows, Python 3.12.10, зависимости из `backend/requirements.txt`, тесты в отдельной локальной ветке `qa/acceptance-checks`.

**76 проверок: 50 прошли, 26 упали, ошибок сбора и пропущенных тестов нет.** Включены 68 новых приёмочных случаев и 8 существующих тестов команды. Все 8 существующих тестов прошли. Продолжительность финального прогона — 14,33 секунды. Падения ниже объединены по причинам; 26 сценариев не означают 26 независимых дефектов.

Команда воспроизведения из корня:

```powershell
./.venv/Scripts/python.exe qa/run.py
```

Повторить один пункт: добавить `-k A06`, `-k E09` и т.д. Новый результат должен ссылаться на новый SHA; этот документ не утверждает, что проблема остаётся в более поздней версии.

## Сохранность решений и входных данных

| Тесты | Шаги | Фактический результат | Ожидание | Где смотреть |
| --- | --- | --- | --- | --- |
| A06, A07 | Скорректировать заказ до 36, утвердить; отдельно пересчитать и перезапустить приложение | Вместо `APPROVED, 36` получаем `DRAFT, 108` | Сохранить заказ/его утверждённую версию и историю; новый расчёт не уничтожает решение | `main.py`: lifespan/calculate; `repositories/database.py`: save_recommendations |
| A03 | После валидного набора загрузить единственную строку остатка −100 | Ошибка в ответе есть, но действующий остаток всё равно заменён на −100 | Отклонить все ошибочные строки, сохранить предыдущий валидный набор | `main.py`: upload; `services/validation.py` |
| A19 | Установить один stockout; загрузить валидный CSV только с заголовками | Ответ без ошибок, старый период остаётся | Успешная пустая замена очищает предыдущие периоды | `main.py`: условие `if len(cleaned)` |

## Расчёт и обязательные критерии кейса

| Тесты | Контрольный сценарий | Фактический результат | Ожидание | Где смотреть |
| --- | --- | --- | --- | --- |
| A17 | Русские заголовки EKT, валидная строка продажи | Загружено 0 строк, отсутствуют английские обязательные колонки | Поддержать согласованный входной формат/адаптер EKT | read_table/validate_table/upload |
| E09, E10, A15 | Стабильно 10/день, 16 дней полного stockout, строк продаж за эти дни нет | Lost demand = 0; на графике 0 отмеченных дней | Lost demand = 160, календарь включает 16 дней, скорректированный спрос положителен | replenishment: ежедневная агрегация; analytics |
| E11 | К периоду 1–16 марта добавить вложенный 5–10 марта | Lost demand падает со 160 до 100 | Объединение интервалов сохраняет 160 | stockout correction |
| E13 | Одинаковая история: в одном варианте выходные заданы нулями, в другом строк нет | Прогноз 70,35 против 94,12 | Эквивалентная календарная история даёт одинаковый прогноз | заполнение календаря до forecast_series |
| E06 | Спрос на ближайшие 10 дней = 100; запас 0; партия 1000 через год | Заказ 0 | Заказ 100, поздняя поставка не покрывает ранний спрос | replenishment: фильтрация inbound по дате |
| E17 | Год истории: зимой 165/день, остальные месяцы 100; прогноз с 1 ноября | Средний прогноз 87,08/день — спад вместо зимнего роста | В этом тесте принято ≥130/день; точный коэффициент не требуется | forecasting: календарная сезонность и отделение тренда |
| E15 | Разовая закупка клиента 4800 разделена на 480 строк по 10 в один день | Выбросов 0 | Выявить аномальную закупку после подходящей агрегации; исходные строки сохранить | уровень агрегации MAD |
| E05 | Потребность 99,5, Pack=1 | Заказ 99,5 | Заказ 100 при заданной единичной кратности | округление упаковки при Pack=1 |
| A08 (3 случая) | MOQ=24, Pack=12; сохранить и утвердить 1, 25 или 24,5 | Все три значения становятся APPROVED | Некорректный черновик допустим, но утверждение должно блокироваться без формального исключения | adjust_order / approve_order |

E17 содержит выбранный допуск качества; его необходимо согласовать как приёмочный порог. Это не математическая гарантия точного восстановления годовой сезонности по одному циклу. E15 задаёт конкретную политику агрегирования клиент/день; регулярные крупные клиенты проверяются отдельно.

## Согласованность интерфейса, аудита и экспорта

| Тесты | Наблюдение | Ожидание |
| --- | --- | --- |
| A14 | На растущем спросе первые 10 дней графика дают 514,08, расчёт заказа использует 397,46 | График строится из тех же рассчитанных точек, сумма совпадает с учётом округления |
| E16 | Аудит одного товара содержит выброс чужого SKU | Аудит конкретной рекомендации содержит только относящиеся к ней SKU/склад |
| A10 | Итог изменён со 108 на 36, KPI остаётся 108 | Если карточка означает «к заказу», отображать 36; либо явно отделить исходную рекомендацию от итогового заказа |
| A11 | Наименование `=1+1` экспортируется в XLSX как формула | Пользовательский текст остаётся текстом; формулы разрешены только в расчётных полях |

A10 — требование к смыслу карточки, которое зависит от принятого UX-контракта; сама сумма исходных рекомендаций может быть полезным отдельным показателем.

## Валидация

| Тесты | Принятые без ошибки значения | Требуемая реакция |
| --- | --- | --- |
| V01 (3 случая) | `stock.current_stock=inf`, `sales.quantity=inf`, `package_size=0` | Явная ошибка до применения данных |
| V02 | Два разных остатка одной пары товар/склад | Отклонить конфликт или применить документированное правило выбора по времени; не брать случайно первую строку |
| V03 | Начало stockout позже окончания | Отклонить интервал |

Здесь зафиксирована недостаточная валидация, а не утверждение, что каждый такой ввод обязательно приводит к HTTP 500. Отдельный сценарий устойчивости после строки с `inf` прошёл.

## Что уже работает в проверенных сценариях

Ручной расчёт с MOQ/упаковкой; невозрастание заказа при увеличении запасов или своевременных поставок; нулевая потребность при достаточном запасе; stockout с явными нулями; одиночный большой заказ в одной строке; рост спроса; изоляция складских остатков; короткая и пустая история; неизменность входных таблиц; контроль отрицательных параметров API; повреждённый Excel не затирает набор; экспорт сохранённого итогового количества и статуса; повторное утверждение не дублирует строки.

## Очерёдность для команды

1. Сохранность утверждений и атомарность импорта: A03, A06, A07, A19.
2. Входной формат и MUST HAVE: A17, E09/E10, E11, E13, E06, E17, E15, E05/A08.
3. Единый источник данных для графика, объяснения и экспорта: A14/A15, E16, A10/A11.
4. Валидация V01/V02/V03 и ручной маршрут из ACCEPTANCE.md.

Код приложения в рамках QA не исправлялся. Docker, нагрузка, одновременные запросы и браузерный маршрут в этом отчёте не проверены. Docker CLI на этой машине не найден. Замечания не отправлялись коллегам отдельными сообщениями; отчёт доступен вместе с тестами в QA-ветке.
