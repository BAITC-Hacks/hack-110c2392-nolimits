# Проверка исправленной ветки

Основа: `main` на `a3b3d00` (23 сентября 2026). Ветка `fix/replenishment-qa` включает изменения команды в `main` до этого SHA и исправления дефектов из [исторического аудита](CURRENT_MAIN_AUDIT.md).

| Проверка | Результат |
| --- | --- |
| `python -m pytest -q qa backend/tests` | **97 passed**, 2 предупреждения зависимостей/парсинга даты, 304,70 с |
| `python -m pytest -q qa` после добавления даты среза | **74 passed**, 2 предупреждения |
| `npm run build` в `frontend/` | TypeScript и Vite собраны; предупреждение о JS-чанке >500 КБ |
| Стресс-книга `ekt_extreme_anomalies_sales_and_stock.xlsx` | 20 294 транзакции, 57 рекомендаций, 46 аномальных клиентских заказов; тендер 8500 м в аудите, прямая отгрузка 5000 м исключена, неликвид не заказан |
| `docker compose up --build` | Не запускался: Docker CLI отсутствует на этой машине. Добавлены `.dockerignore` и детерминированный `npm ci`; запуск контейнеров остаётся обязательной проверкой на машине с Docker. |

Все 25 падений исходного аудита закрыты. Дополнительно проверены сверка сторно с исходной продажей, прямая отгрузка мимо склада, минимальная сумма заказа, архивирование утверждения при смене исходных данных, атомарный отказ от некорректной Excel-книги, риск дефицита до прибытия партии и фактическая дата среза на дашборде.

Проверки API используют временную SQLite. Результаты не доказывают точность прогноза на скрытых данных: для промышленного решения потребуется ретроспективный backtest и согласование политики просроченных поставок.
